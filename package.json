{
  "name": "visit-wheel",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:all": "npm run build:click && npm run build:auto",
    "deploy:all": "npm run build:all && firebase deploy",
    "build:click": "vite build --mode click",
    "build:auto": "vite build --mode auto",
    "deploy:click": "npm run build:click && firebase deploy --only hosting:wheel-visit-click",
    "deploy:auto": "npm run build:auto && firebase deploy --only hosting:wheel-visit-auto"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "vite": "^7.1.2"
  },
  "dependencies": {
    "gsap": "^3.13.0"
  }
}
